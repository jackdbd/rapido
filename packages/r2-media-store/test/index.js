import { describe, it } from 'node:test'

describe('Cloudflare R2 media store', () => {
  it.todo('test content-type to filename (update.ts)')
  it.todo('test url split to bucket path (delete.ts)')
})
