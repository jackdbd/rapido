meta {
  name: exchange auth code for access token (no media)
  type: http
  seq: 3
}

post {
  url: {{token_endpoint}}
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  grant_type: authorization_code
  client_id: {{client_id}}
  code: {{code_no_media_scope}}
  code_verifier: {{code_verifier}}
  redirect_uri: {{redirect_uri}}
}
