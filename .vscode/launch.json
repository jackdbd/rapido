{
  "version": "0.2.0",
  "configurations": [
    {
      // First, launch the Node.js application with one of:
      // * node --inspect
      // * node --inspect-brk
      // * NODE_OPTIONS='--inspect' node
      // * NODE_OPTIONS='--inspect-brk' node
      // Then attach to it.
      "name": "Attach to Node.js process",
      "request": "attach",
      "type": "node",
      "port": 9229,
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
