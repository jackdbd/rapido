meta {
  name: syndicate (JSON)
  type: http
  seq: 2
}

post {
  url: {{syndicate_endpoint}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "feed": "{{notes_feed}}"
  }
}
